name: Django CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.11]
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        echo SECRET_KEY=django-insecure-3b5l*tmmbv_6!b$!=%-zhp6avf#_k_o$0$eimx@w_xyto^$1wx > .env
        echo EMAIL_PASSWORD=vouka2012 >> .env
        echo EMAIL_SENDER=vouka7@yandex.ru >> .env
        ls
        ls
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
        python3 manage.py makemigrations
        python3 manage.py migrate
    - name: Run Tests
      run: |
        pytest